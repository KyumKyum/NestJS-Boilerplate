//* TODO: Need to add kafka environments in here.
//* Current definition is marginal; There will be more envs required. Please add after the setting is done.

export type KafkaConfigType = {
    host: string;
    brokers: string[];
};
